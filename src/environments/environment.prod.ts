import { Environment } from './environment.types';

export const environment: Environment = {
  production: true,
  channelId: 1055033,
  tiles: [{
    fieldId: 'field1',
    tileType: 'weight'
  }]
};
